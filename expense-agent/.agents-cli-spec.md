# Agent Spec: Ambient Expense Agent

## Overview

An ADK 2.0 graph-based ambient agent that processes employee expense reports.

- **Expenses < $100** → `auto_approve` node: instantly approved, structured log emitted.
- **Expenses ≥ $100** → `review_agent` (LLM) analyses risk, then `request_approval` pauses via RequestInput (HITL). Manager reply flows to `process_decision`.

## Graph Topology

```
START → parse_expense → route_by_amount
                              ├──[AUTO_APPROVE]──► auto_approve
                              └──[NEEDS_REVIEW]──► review_agent → request_approval → process_decision
```

## Reference Samples

Based on `ambient-expense-agent` from `google/adk-samples`.
